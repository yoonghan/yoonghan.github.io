import React from 'react';

interface ICardProps {
  imgSrc: string;
  desc: string;
  title: string;
  link: string;
}

const Card: React.SFC<ICardProps> = ({imgSrc, desc, title, link}) => (
  <div>
  </div>
);
