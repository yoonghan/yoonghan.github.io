export const metadata = {
  title: "Performance",
  description: "Performance testings.",
}

export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
