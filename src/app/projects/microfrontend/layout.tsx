export const metadata = {
  title: "Microfrontend",
  description: "Microfrontend architecture.",
}

export default function MicrofrontEndLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
